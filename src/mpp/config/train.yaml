# ============================================================
# Train config for scripts/train.py
# Values here take precedence over cadtoseq.yaml, but are
# overridden by explicit CLI arguments.
# ============================================================

model:
  lr: 9.77e-4
  embed_dim: 96
  nhead: 16
  num_layers: 3
  dropout: 0.265
